"use client"

// Zero logiki tutaj — patrz .claude/skills/code-tile/SKILL.md.
// UI komponenty wyłącznie z @cortex/ui, wywołania LLM wyłącznie przez
// api/{{id}}/generate (code-api -> code-integration), nigdy fetch bezpośrednio.

export default function {{pascalCase id}}Page() {
  return null // TODO: zbuduj UI kafelka tutaj
}
