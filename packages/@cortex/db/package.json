{
  "name": "@cortex/db",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc --noEmit",
    "db:generate": "drizzle-kit generate --config drizzle.konfiguracja-systemu.config.ts",
    "db:migrate": "drizzle-kit migrate --config drizzle.konfiguracja-systemu.config.ts",
    "db:seed": "node scripts/seed-konfiguracja-systemu.mjs"
  },
  "dependencies": {
    "drizzle-orm": "^0.36.0",
    "postgres": "^3.4.0"
  },
  "devDependencies": {
    "@cortex/tile-sdk": "workspace:*",
    "drizzle-kit": "^0.28.0"
  }
}
